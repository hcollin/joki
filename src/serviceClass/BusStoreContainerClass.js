
import './BusStoreContainer';

export default class BusStoreContainerClass {

    constructor() {
        this._busStoreContainer = null;
        this._busStoreServiceId = null;
    }

    connectToBus(busStore, serviceId) {
        this._busStoreContainer = BusSToreContainer(serviceId, )
    }

}